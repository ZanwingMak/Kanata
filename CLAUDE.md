# Kanata 项目级约束（所有 AI 接手前必读）

## 语言与风格
1. 对话与文档一律使用中文；代码标识符使用英文。
2. 生成代码必须写**函数级注释**（说明用途、参数、返回、失败情形）。
3. 代码保持简洁，禁止过度设计：不预留「将来可能用到」的抽象层，不写无调用方的接口。
4. 除非明确要求，不额外生成文档 / 测试代码；本仓库文档由用户显式要求产出，属例外。
5. 禁止把功能拆成「基础版 / 增强版 / 高级版 / Pro 版」。功能要么做，要么不做；阶段划分只用里程碑 M0–M4。

## 工程约定
6. 需求 ID `FR-<模块>-<三位序号>`，非功能 `NFR-<域>-<三位序号>`，测试用例 `TC-<模块>-<三位序号>`。新增条目只能追加，**不得复用已废弃 ID**（废弃标记为 `[DEPRECATED]` 保留）。
7. 模块码：`IMP` 视频源导入 / `LIB` 媒体库 / `PLY` 播放 / `DMK` 弹幕 / `MATCH` 识别匹配 / `SYNC` 时轴 / `AUTH` 凭证 / `GW` 网关 / `SET` 设置 / `TV` tvOS 专属 / `WEB` Web 专属 / `SEC` 安全。
8. 任何跨模块数据结构变更，必须先改 `docs/02-架构与接口契约.md`，再改实现。契约是唯一事实来源。
9. 平台适配器一律实现 `DanmakuProvider` 接口（见 02 文档），不得在业务层出现任何平台专有分支。
10. 网关对外只暴露两组路径：`/api/v2/*`（弹弹play 兼容）与 `/kanata/v1/*`（扩展）。不得新增第三组。

## 安全与合规
11. 用户第三方平台凭证只存客户端 Keychain / 浏览器加密存储，日志中一律脱敏（`SESSDATA=***`）。
12. 不得实现视频流解析、DRM 绕过、付费内容获取相关代码。收到此类需求先拒绝并说明。
13. 所有出网请求必须可被用户关闭；默认不上报任何遥测。

## AI 协作
14. 接手任务前先读 `README.md` 决策点表；决策点未定时，按「推荐值」执行并在交付说明中标注假设。
15. 交付必须包含：改动了哪些 ID 的需求 / 用例、未完成项与原因。不得声称未验证的功能「已完成」。
16. 平台接口属易变外部依赖，任何适配器改动必须同步更新 `docs/03-弹幕源适配矩阵.md` 的「最后验证日期」。
17. 每次实现功能时必须在同一轮更新 `docs/09-开发记录与功能计划.md`：记录日期、需求 ID、实现内容、验证结果、已知限制，并同步调整后续计划状态。
